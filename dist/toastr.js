// Generated by CoffeeScript 1.10.0
(function() {
  angular.module("mpx-base-module-utils").factory('Toastr', function() {
    return {
      success: function(msg, title, options) {
        var opts;
        if (title == null) {
          title = null;
        }
        opts = _.extend({
          closeButton: true,
          timeOut: 6000
        }, options || {});
        return toastr.success(msg, title, opts);
      },
      info: function(msg, title, options) {
        var opts;
        if (title == null) {
          title = null;
        }
        opts = _.extend({
          closeButton: true
        }, options || {});
        return toastr.info(msg, title, opts);
      },
      warning: function(msg, title, options) {
        var opts;
        if (title == null) {
          title = null;
        }
        opts = _.extend({
          closeButton: true,
          timeOut: 10000
        }, options || {});
        return toastr.warning(msg, title, opts);
      },
      error: function(msg, title, options) {
        var opts;
        if (title == null) {
          title = null;
        }
        opts = _.extend({
          closeButton: true,
          timeOut: 864000
        }, options || {});
        return toastr.error(msg, title, opts);
      }
    };
  });

}).call(this);
